# 《像素货架》— 超市货架放置计分小游戏 · 设计文档

> 一句话定位：**在一间像素小店里，把货架摆到满、摆到分最高。** 一款 Islanders 式的放置计分游戏：抽货架卡、放进网格、靠相邻关系与通道布局得分，分够了解锁更多货架与促销卡，摆满即开业结算。单局 5~8 分钟，规则一句话讲完，每日挑战 + emoji 分享，纯前端静态站点，直接托管 GitHub Pages，无 WebGL 依赖。

母项目（supermarket-2D3D）是零售设计师用的布局工作台，沉淀了货架目录、真实尺寸、碰撞规则和"端架是黄金位 / 冷链要成排 / 通道要够宽"等陈列知识。本游戏**不做经营模拟**，而是把这些知识直接翻译成计分规则——玩家在追高分的过程中，摆出来的就是一间像真超市的店。

---

## 1. 为什么好玩（乐趣支柱）

| 支柱 | 说明 | 参考 |
|---|---|---|
| **放下即反馈** | 每放一件货架立刻飘分、相邻加成连线闪一下。没有"等一天看报表"的延迟。 | Islanders、Dorfromantik |
| **有限空间的取舍** | 通道留 1 格紧凑但少加成，留 2 格舒适但浪费面积；端架翻倍但要先有双面架撑；冷柜要贴电源墙。每一手都在算。 | Backpack Hero |
| **摆满的整理癖** | 网格吸附、货架朝向、通道整齐。摆完的店面截图本身就是奖励。 | Unpacking、A Little to the Left |
| **每局不同** | 随机牌序 + 促销卡（局内规则修正）让同一张店面每局思路不同。 | Balatro、Luck be a Landlord |
| **可分享** | 每日挑战全球同种子，结果是一串 emoji 网格 + 分数 + 可复现链接，贴群里比拼。 | Wordle |

**差异化**：市面超市游戏重在收银/经营；本游戏是"零售陈列学 × 放置解谜"，规则来自真实门店设计经验，别人做不出这个味道。

---

## 2. 核心循环（一局 ≈ 5~8 分钟）

```
┌─ 抽卡 ──────────────────────────────────────────┐
│  手牌 3 张货架卡（从本局牌库抽）                      │
└──────────────────┬──────────────────────────────┘
                   ▼
┌─ 放置 ──────────────────────────────────────────┐
│  选一张 → 在网格上移动幽灵预览（合法绿 / 非法红）        │
│  → R 旋转 → 点击落地 → 飘分 + 相邻加成连线闪烁          │
│  → 补一张手牌                                      │
└──────────────────┬──────────────────────────────┘
                   ▼
┌─ 解锁（分数过阈值时触发）────────────────────────────┐
│  牌库加入新货架种类；三选一促销卡（本局生效的规则修正）    │
└──────────────────┬──────────────────────────────┘
                   ▼
┌─ 开业结算（手牌都放不下 / 主动点"开业"）────────────────┐
│  门打开 → 像素小人涌入沿通道走动、拿货、结账（纯演出）     │
│  → 计分板：基础分 / 相邻加成 / 通道评分 / 浪费扣分        │
│  → 生成分享卡（emoji 网格 + URL）                     │
└─────────────────────────────────────────────────┘
```

---

## 3. 规则系统（游戏的心脏）

### 3.1 场地

- 网格 **14 列 × 10 行**（1 格 ≈ 0.6 m，对应 8.4 m × 6 m 小店），四周是墙。
- **门**在底边中央，宽 2 格；门前 2 行 × 4 列是"门口缓冲区"，不可放置（对应母项目出入口缓冲区规则）。
- **电源墙**：三面墙中随机 1~2 段（每段 4 格长，贴电箱贴纸）。冷柜必须至少有 1 格贴电源墙。
- 关卡模式会出现 L 形、带柱子、双门的变体场地；无尽模式场地随分数逐步扩展（见 3.6）。

### 3.2 货架积木

所有货架都是整数格积木，并且**有朝向**——哪些边是"取货面"决定它如何得分。基础分按母项目真实价格比例折算。

| 货架 | 占格 | 取货面 | 放置约束 | 基础分 | 对应零售学 |
|---|---|---|---|---|---|
| 双面货架 `double-gondola` | 2×1 | 前 + 后（两长边） | 无 | 60 | 主陈列区主力 |
| 单面货架 `single-wall` | 2×1 | 前（一长边） | 背面必须贴墙 | 40 | 靠墙陈列 |
| 端架 `endcap` | 1×1 | 朝外一面 | 必须贴双面货架的短边端头 | 30 → 有效 ×2 | 端架 = 黄金位 |
| 仓储架 `warehouse-rack` | 3×1 | 前（一长边） | 背面必须贴墙 | 110 | 仓储陈列 |
| 立式冷柜 `upright-chiller` | 2×1 | 前 | 背面必须贴**电源墙** | 120 | 冷链区 |
| 卧式冰柜 `island-freezer` | 2×1 | 前 + 后 | 曼哈顿距离 2 格内必须有立式冷柜 | 100 | 冷链共用线路 |
| 木架 `wood-display` | 2×2 | 四面 | 无 | 90 | 生鲜烘焙聚人气 |
| 促销堆头 `promo-display` | 1×1 | 四面 | 无 | 20 → 在主动线上 ×3 | 冲动消费 |
| 收银台 `checkout` | 2×1 | 前 | 距门 ≤ 3 格；每局必放且仅一张 | 0（解锁"开业"） | 出口结算 |

`v0.1` 起始牌库：双面货架、单面货架、端架、堆头；收银台不进牌库，是手牌栏的常驻槽位，放好即消失。开局保底手里至少 1 张双面货架。其余货架随分数解锁（3.5）。

**兜底**：若手牌（含收银台）已无处可放而收银台未放，允许直接开业，但总分减半——避免把门口填死后的软锁。

### 3.3 通道规则（让它不只是拼图的关键）

1. **取货面必须朝通道**：取货面正前方那一格必须是空地，否则该面是"死面"，不计分。这自然逼出"货架 + 通道"的节奏。
2. **空地必须可达**：所有空地必须能从门泛洪走到（4 邻域 BFS）。被圈死的空地红闪提示，结算时按"浪费面积"扣分。
3. **通道宽度**：取货面前方连续空地恰好 1 格 = **紧凑**（无加成）；2 格 = **舒适**（该面 +10%）；≥3 格 = **空旷**（该面 +10%，但多出的格子按浪费计）。对应母项目 dense / balanced / comfortable 三档。
4. **主动线**：门到"最远可达空地"的 BFS 最短路径集合就是主动线。堆头落在主动线上 ×3；端架落在主动线旁 +20%。

### 3.4 相邻加成

| 组合 | 条件 | 效果 |
|---|---|---|
| 联排 | 同类货架长边贴长边 | 每多一件 +10%，最多 +40%（联排计价的反向：越整齐越值） |
| 冷链区 | ≥2 台立式冷柜连排 | 每台 +50% |
| 端头黄金位 | 端架贴双面货架端头 | 端架 ×2；若该双面架处于联排 ≥3 件，再 +20% |
| 入口聚人气 | 木架距门 ≤ 4 格 | +50%；≤ 2 格 +100% |
| 生鲜冷链相邻 | 木架与卧式冰柜相邻 | 双方 +20% |
| 冷热冲突 | 冷柜与仓储架相邻 | 冷柜 −30%（仓储区灰尘、温度） |

每次放置只计算**新放置件及其受影响的邻居**的分差，结算时全量重算校验。

### 3.5 牌库、解锁与促销卡

- 每局牌库 36 张，抽牌时按"当前已解锁种类 × 权重"用种子随机决定，手牌 3 张，放一补一；可丢弃 3 次（丢一张补一张）。
- **分数阈值解锁**（Islanders 式成长）：
  - 400 分 → 立式冷柜、仓储架
  - 1000 分 → 木架、卧式冰柜
  - 1800 分 → 促销卡第三张（v0.2 加稀有卡：双端架、转角架）
- 每次解锁同时**三选一促销卡**（本局生效，Balatro 式规则修正），示例：
  - 「冰爽夏日」：冷柜不再要求电源墙
  - 「黄金端头」：端架 ×3
  - 「窄巷高手」：紧凑通道也 +10%
  - 「大促周」：堆头四面都算主动线
  - 「双倍收银」：可放第二张收银台，收银台 4 格内货架 +15%
  - 「清仓」：手牌变 4 张
- 促销卡上限 3 张；牌库抽干或手牌全部放不下即进入结算。

### 3.6 模式

1. **每日挑战**（主打）：`seed = YYYYMMDD`，全球同场地、同电源墙、同牌序、同促销卡选项。一天一局，结果可分享。
2. **无尽模式**：牌库抽干或手牌无处可放时场地向右扩 2 列（最多 20 列），补 14 张牌、加 1 次丢弃，继续追总分与最高分（本地）。
3. **关卡模式**（v0.3）：20 张手工店面（L 形、柱子、双门、超长走廊），固定牌序，三星评分 = 目标分的 60% / 80% / 100%。
4. **自由摆**：全解锁、无分数，纯收纳癖与截图用。

### 3.7 结算与分享

- 计分板分项：基础分、相邻加成、通道评分、浪费扣分（每格 −5）、死面扣分（每面 −15）、促销卡加成。
- **emoji 分享卡**（复制到剪贴板）：

```
像素货架 #247  ⭐ 1,860
🟩🟩🟨⬜🟩🟩🟨⬜🟦🟦
⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜
🟩🟩🟨⬜🟫🟫⬜⬜🟦🟦
...
https://<user>.github.io/sm-github/#d247-eJyrVkrOz1...
```

- URL hash 格式 `#d20260917.<base64url(deflate(actions))>`：记录的是**动作序列**（放置 / 选促销卡 / 丢弃 / 开业），打开时用同种子回放引擎重建整家店，分数必然一致；未开业的局可以接着玩（对应母项目分享方案）。
- 同一套动作序列也用于 localStorage 存档：只存动作，不存棋盘快照。

---

## 4. 像素风视觉

### 4.1 视角与规格

- **3/4 斜俯视**（Stardew Valley / 宝可梦式），不用正俯视。正俯视货架只剩色块；斜俯视能看到正面一层层商品，这是"精致"感的全部来源。
- 基础格 **16×16 px**，地面格贴图 16×16；货架 sprite 宽 = 占格 × 16，高 = 16 + 立面高度（双面架 16×32，仓储架 48×40，冷柜 32×40）。
- 画布逻辑分辨率 14×16 + 墙 = 236 px 宽，按**设备像素**整数倍放大填满窗口（高分屏手机上也能占满宽度且保持锐利），`image-rendering: pixelated`。
- 主动线用橙色"脚印点"而不是整格铺色，空店面时不会糊成一片。
- 调色板统一用 **Endesga 32**（或 Resurrect 64 子集），全部 sprite 不越界。

### 4.2 内容变体与微动效

- 商品贴图变体：同一种货架有 3 套正面（零食 / 饮料 / 日化 / 生鲜…），落地随机选，画面不重复。
- 落地：挤压反弹 2 帧 + 尘土 3 帧；得分数字像素字体上飘。
- 冷柜门霜光 2 帧循环；收银台屏幕 2 帧闪烁；木架上的水果偶尔掉一颗（彩蛋）。
- 开业演出：门开 → 8~16 个像素小人（4 帧走路循环）在通道里泛洪式游走、在取货面前停 0.5 s 伸手、到收银台排队、出门。纯装饰，不影响分数。
- 幽灵预览：合法半透明绿、非法半透明红 + 抖动 1 帧；相邻加成用 1 px 亮线连到受益货架。

### 4.3 素材策略

1. **v0.1 程序化生成**：用 Canvas 描点函数按调色板画出每种货架（延续现有 SVG ShelfArt 的思路），零授权风险、随时改尺寸。
2. **v0.3 替换为手绘图集**：Aseprite 出 `atlas.png` + JSON，渲染层只换加载器。
3. **CC0 补充**：Kenney Tiny Town / Roguelike Modern City 里的货架、收银机、冰柜可改色拼用；人物用 Kenney Tiny Dungeon 角色改色。

---

## 5. 技术方案

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | Vite + React 19 + TypeScript | 与母项目同栈，现有脚手架直接用 |
| 状态 | Zustand | 现有 `store.ts` 模式保留；棋盘 / 牌库 / 进度分 slice |
| 棋盘渲染 | 单个 `<canvas>` + Canvas 2D | 无 WebGL 依赖，低端手机可跑；按状态变化重绘，仅动效期间开 rAF |
| HUD / 菜单 | React DOM | 手牌、促销卡、计分板、分享弹窗 |
| 规则引擎 | 纯 TS 函数（网格数组 + 规则表） | 无渲染依赖，vitest 全量单测 |
| 随机 | 自写 mulberry32 种子 PRNG | 每日挑战需可复现 |
| 存档 | localStorage | 进度、最高分、今日是否已玩 |
| 分享 | fflate 压缩 JSON → URL hash | 无后端 |
| 部署 | GitHub Actions → GitHub Pages | 现有 workflow 保留 |

**移除**：`three`、`@react-three/fiber`、`@react-three/drei`（约 600 KB 依赖）、`public/models/*.glb`。

**性能目标**：首屏 gzip < 300 KB、无网络请求、首屏 < 1 s、手机端 60 fps（放置 + 开业演出）。

### 5.1 目录规划

```
sm-github/
├─ index.html
├─ vite.config.ts              # base: '/sm-github/'
├─ .github/workflows/deploy.yml
├─ src/
│  ├─ game/                    # 规则引擎（纯数据，零渲染依赖）
│  │  ├─ catalog.ts            #   货架定义：占格 / 取货面 / 约束 / 基础分
│  │  ├─ grid.ts               #   网格模型、放置合法性、旋转
│  │  ├─ flow.ts               #   泛洪可达、主动线 BFS、通道宽度
│  │  ├─ scoring.ts            #   基础分 / 相邻加成 / 通道评分 / 扣分
│  │  ├─ deck.ts               #   牌库生成、抽卡、解锁阈值、促销卡
│  │  ├─ promo.ts              #   促销卡定义与规则修正
│  │  ├─ rng.ts                #   mulberry32 种子随机
│  │  ├─ share.ts              #   emoji 卡 + URL hash 编解码
│  │  ├─ persistence.ts        #   localStorage
│  │  └─ store.ts              #   Zustand
│  ├─ render/
│  │  ├─ Board.tsx             #   canvas 挂载、输入映射（格坐标）、缩放
│  │  ├─ sprites.ts            #   程序化像素 sprite 生成 / 图集加载
│  │  ├─ draw.ts               #   地面、货架、幽灵、连线、飘分
│  │  └─ crowd.ts              #   开业小人演出
│  ├─ ui/                      # 手牌栏 / 促销卡三选一 / 顶栏 / 计分板 / 分享 / 帮助
│  └─ App.tsx
└─ public/
   └─ atlas/                   # v0.3 手绘图集（v0.1 为空）
```

### 5.2 复用与丢弃

| 现有文件 | 处置 |
|---|---|
| `src/game/catalog.ts` | 保留货架种类、颜色、尺寸比例；重写为占格 / 取货面 / 约束 / 基础分 |
| `canPlaceAt` 越界 / 门口缓冲 / 重叠逻辑 | 改为网格版本沿用 |
| `src/game/store.ts`、`persistence.ts` | 结构保留，替换 slice 内容 |
| `.github/workflows/deploy.yml`、`vite.config.ts` | 原样保留 |
| `src/game/sim.ts`、`pathfinding.ts`、`src/sim3d/`、`src/editor2d/` | 删除（顾客模拟 / A* / 3D / SVG 编辑器不再需要） |
| `public/models/*.glb` | 删除 |
| 母项目 `SpatialRelation`、三档通道参数 | 作为相邻加成与通道宽度规则的数值来源 |

---

## 6. 版本规划

| 版本 | 内容 | 验收标准 |
|---|---|---|
| **v0.1 MVP** | 14×10 场地 + 电源墙、9 种货架（含解锁）、通道 / 可达 / 主动线 / 相邻加成计分、手牌 3 张抽卡、阈值解锁 + 促销卡三选一（6 张）、开业小人演出、每日挑战 + 无尽模式、emoji / URL 分享、localStorage、程序化像素 sprite、Pages 部署 | 手机浏览器打开 < 1 s；1 分钟内看懂规则；单局 5~8 分钟；每日挑战结果可贴群比拼 |
| **v0.2** | 稀有卡（双端架、转角架）、促销卡扩至 15 张、无尽模式扩场地、成就 / 图鉴、音效 | 无尽模式单局 30 分钟不无聊 |
| **v0.3** | 关卡模式 20 关（L 形 / 柱子 / 双门）、手绘图集替换、自由摆模式、本地最高分榜 | 关卡通关率与三星分布合理 |
| **v1.0** | 数值调优、多语言（中 / 英）、PWA 离线、正式发布 | — |

**v0.1 明确不做**：3D、顾客模拟、经济系统、多人、后端、账号。

---

## 7. GitHub Pages 部署要点

1. `vite.config.ts` 的 `base: '/sm-github/'`（仓库名即子路径，改名同步改）。
2. `.github/workflows/deploy.yml`：push main → `pnpm install && pnpm build` → `actions/deploy-pages`。
3. 仓库 Settings → Pages → Source: GitHub Actions。
4. 全部资源相对路径，运行时零 API 依赖。
5. 分享链接依赖 URL hash，Pages 的 SPA 无需额外路由配置。

---

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 规则太多玩家记不住 | 起始牌库只有 5 种货架、3 条规则（面朝通道 / 端架翻倍 / 收银靠门）；其余随解锁逐条弹出教学；幽灵预览实时显示"为什么红" |
| 最优解固定、重玩性差 | 随机电源墙位置 + 随机牌序 + 促销卡三选一；每日挑战靠"同条件比拼"而不是靠随机 |
| 程序化 sprite 不够精致 | 先用程序化保证迭代速度，v0.3 换手绘图集；渲染层只依赖 `sprites.ts` 接口 |
| 分数通胀 / 后期无聊 | 无尽模式扩场地同时提高解锁阈值；促销卡有上限 3 张 |
| 手机端误触 | 两步放置（先点格子预览，再点确认或拖到位松手）；手牌栏放底部，拇指区可达 |
| 分享链接过长 | 只存 `{seed, placements: [typeIdx, x, y, rot][]}`，fflate + base64url，实测 < 200 字符 |
